import React, { useEffect } from "react";
import style from './PortfolioTotal.module.css';

const PortfolioTotal = (stocks, setStocks) => {

    

    return (
        <div className={style.total}>
            100000000
        </div>
    );
};

export default PortfolioTotal;
